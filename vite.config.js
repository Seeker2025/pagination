import { defineConfig } from 'vite';

export default defineConfig({
  // base must match the GitHub Pages path for repo pages:
  // if your repo is https://Seeker2025.github.io/pagination/
  // set base to '/pagination/'.
  base: '/pagination/',
});
